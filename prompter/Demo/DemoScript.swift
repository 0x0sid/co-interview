import Foundation

/// §12.2's bundled demo script — ~100 words, read aloud during the one-tap demo. Plain
/// Foundation content, no UI dependency, same pattern as `Matching/`'s fixture scripts.
enum DemoScript {
    static let text = """
    Welcome to Prompter. In about thirty seconds, you'll see something most teleprompter apps \
    can't do: this text follows your voice, not the other way around.

    Just start reading at your own pace. If you pause to think, the page waits for you. If you \
    skip ahead, or go off script for a second, it catches up on its own — no tapping, no \
    scrubbing, no assistant standing off camera with a clicker.

    Everything happens right here, on your phone, and nothing you say ever leaves it. That's \
    the whole idea. Now go ahead and try it — read this last line, and watch what happens next.
    """
}
